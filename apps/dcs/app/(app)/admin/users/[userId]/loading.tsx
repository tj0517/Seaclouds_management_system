// DCS roles per user: one card per project, six role checkboxes in each.
import { DetailPageSkeleton } from '@/components/skeletons'

export default function Loading() {
  return <DetailPageSkeleton cards={4} />
}
