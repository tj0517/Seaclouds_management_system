// Clients: Name / Code / Contact email / Projects / Status / Actions.
import { ListPageSkeleton } from '@/components/skeletons'

export default function Loading() {
  return <ListPageSkeleton rows={5} cols={6} />
}
