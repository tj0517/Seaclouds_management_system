{
  "name": "scl-portal",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@10.30.3",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test:unit": "turbo run test:unit",
    "db:gen": "supabase gen types typescript --local > packages/db/src/database.ts && turbo run typecheck"
  },
  "devDependencies": {
    "turbo": "^2.5.8"
  }
}
