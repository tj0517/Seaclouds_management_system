{
  "name": "@scl/docs",
  "version": "0.0.0",
  "private": true,
  "description": "Workspace member on purpose: makes docs/ changes non-global for Vercel's affected-project detection. No code, no scripts, no dependencies."
}
