# Punkty otwarte

O-01…O-10 pochodzą z briefu (sekcja 16); O-11+ dopisane podczas prac.
Agent: jeśli task zależy od punktu „otwarty” — nie zgaduj rozstrzygnięcia;
zaimplementuj tak, żeby decyzja była tania do wprowadzenia, i zgłoś w raporcie.

| Nr | Zagadnienie | Status | Kto decyduje | Co blokuje |
|---|---|---|---|---|
| O-01 | Nazwa portalu nadrzędnego (propozycja: SCL Portal) | otwarty | Sea Clouds (MD) | Tylko naming w UI/repo; prace nie stoją |
| O-02 | Zakres integracji z SCL-TES — wspólny core w Fazie 1a | **rozstrzygnięty** — TES reużyty jako core, [ADR-0001](adr/0001-reuzycie-tes-jako-core.md) | — | — |
| O-03 | Hosting frontendu | **rozstrzygnięty** — Timesheet działa na Vercelu (Root Directory `apps/timesheet`); przyjęcie tego samego dla `apps/dcs` do formalnego potwierdzenia | Wykonawca (rekomendacja: Vercel) | Konfigurację deployu `apps/dcs` (część 2) |
| O-04 | Okres retencji audit logu i kopii zapasowych (RODO) | otwarty | Sea Clouds (MD) | Definicję polityki retencji w `public.audit_log` (tabela istnieje od 2026-09-03, migracja `20260903173128`, DCS 1a.08; kasować może wyłącznie `postgres` — role API nie mają DELETE/TRUNCATE) |
| O-05 | Mapowanie kolorów z kolumny E arkusza SMDR na `workflow_status` | otwarty | Sea Clouds (DC) | Import M14 (test akceptacyjny Fazy 1b) i kolory statusów w widoku MDR — patrz [02-data-model.md](02-data-model.md). **Nie blokuje już rejestru (1b.05):** ekran `/mdr` jeździ na palecie TYMCZASOWEJ, wybranej świadomie, w jednej stałej `MDR_STATUS_COLORS` w `apps/dcs/lib/mdr.ts` (+ neutralny fallback dla kodu spoza listy). Rozstrzygnięcie O-05 to edycja tej jednej stałej albo podmiana jej ciała na odczyt `dcs.dictionaries.meta.color` — żaden inny plik nie zna koloru statusu. Do tego czasu **nie opisuj tych kolorów jako uzgodnionych**; ani jeden z 9 wierszy `workflow_status` nie ma `meta.color` (odczyt scl-dev 2026-09-19) |
| O-06 | Kody CTR wspólne firmowo czy per projekt | otwarty | Sea Clouds (MD) | Sekcję CTR kreatora Create Project MDR. `dcs.documents.ctr_code` **powstało 2026-09-17 (1b.01) w wariancie per projekt** — FK do `public.sub_projects` plus trigger „ten sam projekt"; rozstrzygnięcie „wspólne firmowo" zmieni FK i trigger razem, więc pytanie nadal blokuje, tylko już nie schemat od zera — patrz [02-data-model.md](02-data-model.md) |
| O-07 | Format i szablon transmittalu (wzory klientów?) | otwarty | Sea Clouds (DC) | Moduł M11 (Faza 3); model `dcs.transmittals` można założyć wcześniej |
| O-08 | Zasady zastępstw przy nieobecności Checkera/Approvera | otwarty | Sea Clouds (MD) | Reguły przepisywania zadań w M7 (Faza 2); model `approval_tasks` już to przewiduje (przepisanie przez DC + audit log) |
| O-09 | Automatyczne PDF rendition przy wgraniu pliku źródłowego | otwarty | Sea Clouds (DC) | Zakres M6 (Faza 1b); pole `file_kind = rendition` istnieje niezależnie od decyzji |
| O-10 | Numer i typ dokumentu dla briefu (propozycja SCMS-SCL-SA-0001-PL) | otwarty | Sea Clouds (DC) | Nic w kodzie |

## Dopisane

| Nr | Zagadnienie | Status | Kto decyduje | Co blokuje |
|---|---|---|---|---|
| O-11 | Kody projektów na prod niezgodne z formatem SCYYNN/SCMS: `""` (IT admin), `SCC005`, `SCMS_TEST` (odczyt prod 2026-08-31). Poprawić dane, dopuścić wyjątki, czy wyłączyć te projekty z DCS? | **rozstrzygnięty częściowo** (2026-09-01, migracja `20260901082600_enforce_project_code_format`) — `IT admin` dostał kod `SCMS-IT`; `project_code` jest teraz `NOT NULL` + `CHECK (^SC\d{4}$ OR ^SCMS OR = 'SCC005')`; `SCMS_TEST` pasuje do `^SCMS`. **Zostaje:** `SCC005` (ISO Certyfikacja) jako wyjątek **imienny**, nigdy wzorcowy. Warunek wygaśnięcia: gdy projekt zostanie przenumerowany do formatu SCYYNN albo zarchiwizowany — wtedy usunąć człon `= 'SCC005'` (`docs/deferred-tasks.md`). Walidacja generatora numeracji (M3) osobno. | Sea Clouds (DC/MD) — tylko los `SCC005` | — |
| O-12 | Mapowanie ról projektowych DCS (ORIG/REV/CHK/APP/DC/VIEW) na istniejący globalny enum `user_role` (admin/employee/project_lead) | **rozstrzygnięty** — `user_role` zostaje globalny i należy do TES (bez DC/ADM); role DCS wyłącznie per projekt w `dcs.project_roles` (roboczo `project_members`; nazwa i schemat `dcs` — [ADR-0008](adr/0008-project-roles-w-schemacie-dcs.md), migracja `20260903134914`, DCS 1a.06); `project_lead` niemapowany; administrator DCS = `profiles.role = 'admin'`, [ADR-0006](adr/0006-role-dcs-per-projekt.md) | — | — |
| O-13 | Podział pól konfiguracji MDR między `public.projects` (wspólne: `process_type`, `client_id`, `year`, `status`) a `dcs.mdr_settings` (specyficzne dla DCS) — brief zakłada `core.projects`, ale rozszerzanie tabeli produkcyjnej TES wymaga decyzji, które pola są naprawdę wspólne | **rozstrzygnięty** (2026-09-02, DCS 1a.05, migracje `20260902114742-44`) — `projects` ← `process_type`, `year` (tożsamość wspólna; `client_id` już od 1a.04); `dcs.mdr_settings` (1:1, PK = project_id) ← `cpy_numbering`, trzy cykle, `budget_hours`, `status`. `status` MDR świadomie NIE w `projects`: TES ma `is_active` (logowanie godzin), status MDR = dokumentacja otwarta/zamknięta — uzasadnienie w [02-data-model.md](02-data-model.md) | — | — |
| O-15 | Kolumny stanu przy `dcs.plan_dates` / `dcs.revisions` (`step`): enum Postgres `dcs.step` (jak w projekcie modelu) czy FK do słownika `dcs.dictionaries` typu `workflow_step` (od 1a.07 kroki i statusy obiegu są słownikami, nie enumami)? Enum daje sztywną maszynę stanów i CHECK-i zakresu; FK daje edytowalne etykiety, ale nie wolno pozwolić DC dezaktywować kroku używanego przez maszynę stanów. Zgłoszone 2026-09-04 (DCS 1a.07) | **rozstrzygnięty** (2026-09-17, DCS 1b.01, migracja `20260917130035`) — **FK do słownika**: `dcs.revisions.step_id` → `dcs.dictionaries` typu `workflow_step`, złożonym kluczem obcym `(step_id, step_dict_type)`, więc typ słownika jest pilnowany przez bazę, nie przez trigger. Powód: od 1a.07 każda lista kodów DCS jest słownikiem, którym DC zarządza bez deployu, a sześć kodów (IDC, IFR, RETCOM, IFC, IFI, IFB — bez `START`) jest już zaseedowanych. `dcs.plan_dates` (Faza 2) pójdzie tą samą drogą. **Zostaje:** ryzyko nazwane w treści pytania — nic nie broni DC dezaktywować (`is_active = false`) kroku, którego wymaga maszyna stanów. To zadanie maszyny stanów (Faza 2), nie schematu; `docs/deferred-tasks.md` (oo) | — | — |
| O-14 | 2FA dla ról DC i Administrator — wymóg bezpieczeństwa briefu §3.5; realizacja przez Supabase Auth MFA, konfiguracja przez `config.toml`/migracje, nie dashboard. Zadanie **Fazy 1a, termin 2026-09-15** | otwarty (projektowanie) | wykonawca + MD | Politykę RLS na operacjach DC i wymuszenie aal2 w middleware |
| O-16 | Krąg czytelników plików DCS a członkostwo z Timesheeta. `is_project_member` (`20260903184934:22-37`) zwraca `true` już dla wiersza `public.project_assignments`, bez żadnej roli DCS; wołają ją polityki SELECT rejestru `dcs.documents` / `dcs.revisions` / `dcs.files` (`20260917130035:658,697,736`) oraz `dcs.project_roles` i `public.clients` (`20260903184934:90,113`). **Rozstrzygnięte dla bajtów (2026-09-21, DCS 1b.09 PR 1, PR #80, migracja `20260921112840_create_dcs_documents_bucket`):** polityka SELECT na `storage.objects` dla bucketa `dcs-documents` („Project role holders read dcs documents") **nie** używa `is_project_member` — woła `has_project_role` z pełną, dosłowną listą sześciu wartości `dcs.project_role` (`orig, rev, chk, app, dc, view`, odczyt z produkcji i lokalnie 2026-09-21), więc treść pliku czyta wyłącznie posiadacz wiersza `dcs.project_roles` w tym projekcie albo admin; pracownik przypisany w Timesheecie bez roli DCS dostaje 0 wierszy (test `supabase/tests/storage_dcs_documents.test.sql`, asercja 31, z czerwonym dowodem po przywróceniu `is_project_member`). Lista jest literałem porównywanym w teście z `enum_range`, więc siódma rola nie odziedziczy dostępu bez świadomej edycji polityki. **Zostaje otwarte — metadane:** ten sam pracownik nadal czyta wiersze `dcs.files` (nazwa pliku, rozmiar, kto wgrał, `storage_path`) i cały rejestr `dcs.documents` / `dcs.revisions` w projektach, w których nie ma roli DCS, bo polityki tych tabel zostały celowo bez zmian. Czy metadane mają iść za bajtami (zawężenie trzech polityk SELECT rejestru do `has_project_role`) czy zostać „dla członka projektu" w sensie TES? | **rozstrzygnięty** (2026-09-23, tj po prezentacji Fazy 1): przypisani do projektu w Timesheecie **widzą** listę plików i dane rewizji (metadane `dcs.files` / `dcs.revisions`); bajty plików tylko dla ról DCS (+ admin) — stan z 1b.09 zostaje. Wcześniej: otwarty — metadane | Sea Clouds (MD/DC) | Nic w kodzie; rozstrzygnięcie to jedna migracja z `alter policy` na trzech tabelach rejestru |
| O-17 | Szablon Comment Sheet (SCMS-SCL-LA-0001) — dokładny układ arkusza, do którego eksportowane są komentarze z PDF | otwarty (2026-09-23: klient dośle) | Sea Clouds (DC) | DCS-2.10 |
| O-18 | Technologia podglądu PDF z adnotacjami (komentowanie na rysunku — wybór klienta 2026-09-23) | **rozstrzygnięty** (2026-09-23, tj): własne na **PDF.js** — bez licencji, komentarze jako wiersze w naszej bazie (RLS, dziennik, Comment Sheet); zakres narzędzi: pinezka, obszar, tekst. Narzędzia klasy Bluebeam (chmurki, wymiary, stemple) poza zakresem — gdyby klient ich wymagał, wraca porównanie z SDK komercyjnym | tj | DCS-2.20, 2.21 |
| O-19 | Czy poza Comment Sheet klient chce PDF z wtopionymi komentarzami (do wysyłki dalej)? | otwarty | Sea Clouds (DC) | Ewentualne nowe zadanie (M) po 2.21 |
